interface LaunchPadPageProps {
  params: {
    agencyId: string;
  };
}

const LaunchPadPage = ({ params }: LaunchPadPageProps) => {
  return <div>LaunchPadPage</div>;
};

export default LaunchPadPage;
